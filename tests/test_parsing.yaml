tests:
  equalTo:
    prompt_file: ./en/conversation.txt
    prompt_append: prompt
    assertions:
      - equalTo: "equalTo"
      - similarTo: 
          value: similarTo
          threshold: 0.9
      - startsWith: startsWith
